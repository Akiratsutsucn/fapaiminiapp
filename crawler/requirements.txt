# crawler/requirements.txt
# Extends backend/requirements.txt with crawler-specific dependencies

# Browser Automation
playwright>=1.42.0

# HTML Parsing
beautifulsoup4>=4.12.0
lxml>=5.1.0

# Image Processing
Pillow>=10.0.0

# PDF Parsing（成交确认书「网拍结束时间」提取；扫描件无文本层时自动跳过，不做 OCR）
pdfplumber>=0.11.0

# Install Playwright browsers after pip install:
#   playwright install chromium
