<view class="favorites-page">
  <view class="fav-list">
    <house-card wx:for="{{propertyList}}" wx:key="id" property="{{item}}" wx:if="{{propertyList.length > 0}}" />
    <view class="empty-state" wx:if="{{propertyList.length === 0}}">
      <text>暂无收藏的房源</text>
    </view>
  </view>
</view>
