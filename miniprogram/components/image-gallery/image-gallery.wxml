<view class="gallery">
  <swiper wx:if="{{images.length > 0}}" class="swiper" indicator-dots="{{images.length > 1}}" indicator-color="rgba(255,255,255,0.4)" indicator-active-color="#ffffff" autoplay="{{autoplay}}" interval="4000" duration="400" circular>
    <swiper-item wx:for="{{images}}" wx:key="id">
      <image class="swiper-image" src="{{item.image_url}}" mode="aspectFill" lazy-load binderror="onImageError" data-index="{{index}}" />
    </swiper-item>
  </swiper>
  <view wx:else class="gallery-empty">
    <view class="gallery-empty-art"></view>
    <text class="gallery-empty-text">敬请期待房源实图</text>
  </view>
  <view class="gallery-counter" wx:if="{{images.length > 1}}">{{current + 1}}/{{images.length}}</view>
</view>
